---
description: Turn Codey narration off and restore the plain status line
---

Turn Codey narration off by running:

```bash
node "${CLAUDE_PLUGIN_ROOT}/dist/cli/index.js" off
```

Then tell the user in one line that Codey narration is off and the plain status line is
back. Capture keeps running silently for free.
