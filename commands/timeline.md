---
description: Open the visual storyboard of this session in your browser
---

Run:

```bash
node "${CLAUDE_PLUGIN_ROOT}/dist/cli/index.js" timeline
```

Give the user the http://localhost URL it prints and tell them it opens the visual
storyboard of this session, with a per-task token breakdown. If one is already open it
reuses it.
